# Copy to .env (same folder) for running the service outside Docker. Never commit .env.
# Use the credentials of the PostgreSQL container you started yourself.

APP_ENV=development
DATABASE_URL=postgresql://<user>:<password>@localhost:5432/<database>
INTERNAL_TOKEN=
